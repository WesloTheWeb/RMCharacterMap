import React from 'react';

const HeroBanner = (props) => {
    return (
        <figure className="hero-banner">
            <h2>Rick &amp; Morty Character Map</h2>
        </figure>
    );
};

export default HeroBanner;